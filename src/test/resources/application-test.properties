# --- Spring Batch ------------------------------------------------------------
spring.batch.jdbc.initialize-schema=always
# Do not run the batch job automatically on context start during tests
spring.batch.job.enabled=false
